const composeInputForm = (
  title: string,
  dateStart: Date,
  dateFinish: Date,
  tags: string[],
  description: string,
  file: File,
  position: Location
) => {};
export default composeInputForm;
